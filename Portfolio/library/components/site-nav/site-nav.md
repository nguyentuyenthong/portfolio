# Site Nav

Header sticky cho portfolio / marketing site.

## Anatomy

| Element | Class |
|---------|-------|
| Wrapper | `.uu-site-nav` |
| Brand | `.uu-site-nav__brand` |
| Links | `.uu-site-nav__links` > `.uu-site-nav__link` |
| CTA | `.uu-site-nav__cta` |

## Mobile

`data-uu-menu-open="true"` trên nav + toggle button — xem `demo/portfolio/portfolio.html`.

## Token

Background: `--surface-default` + blur; border `--border-secondary`; link active `--text-brand-secondary`.
